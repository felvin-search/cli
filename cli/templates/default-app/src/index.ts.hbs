import { InstantApp } from "@felvin-search/core";
import { Component, queryToData } from "./App";

const App: InstantApp = {
  id: "@felvin-community/{{appId}}",
  name: "{{appName}}",
  description: "{{appDescription}}",
  queryToData,
  Component,
  // availableOnGoogle: false,
  // screenshotPath: "./files/screenshot.png",
  // exampleSearchQueries: [],
};

export default App;


// check if a similar or better snippet is provided by google for the same query
// and accordingly uncomment and fill in the availableOnGoogle field